import styled from '@emotion/styled';

import colors from '../styles/colors';

const H3 = styled.h3`
  font-size: 2rem;
  font-weight: 400;
  line-height: 1.8rem;
  letter-spacing: 0px;
  color: ${colors.dark};
`;

export default H3;
