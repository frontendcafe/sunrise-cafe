import styled from '@emotion/styled';

import colors from '../styles/colors';

const Link = styled.a`
  font-size: 1.6rem;
  font-weight: 400;
  line-height: 1.8rem;
  letter-spacing: 0px;
  color: ${colors.dark};
`;

export default Link;
